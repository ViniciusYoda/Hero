const a: any = 34;
const b: any = ['Glaucia'];

const result = a + b;
console.log(result);

const formulario: { [campoFomulario: string]: any } = {
  nome: 'Glaucia',
  sobrenome: 'Lemos',
  idade: 34,
};

console.log(formulario);